<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ config('app.name', 'Lunamars') }} - Trouvez le prestataire idéal près de chez vous</title>

    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --primary: #2563eb;
            --primary-dark: #1d4ed8;
            --secondary: #7c3aed;
            --accent: #f59e0b;
            --dark: #0f172a;
            --gray-50: #f8fafc;
            --gray-100: #f1f5f9;
            --gray-200: #e2e8f0;
            --gray-500: #64748b;
            --gray-700: #334155;
            --gray-900: #0f172a;
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--gray-50);
            color: var(--gray-700);
            overflow-x: hidden;
        }
        
        /* ===== NAVBAR ===== */
        .navbar {
            padding: 12px 0;
            z-index: 9999;
            backdrop-filter: blur(20px);
            background: rgba(255,255,255,0.95) !important;
            border-bottom: 1px solid var(--gray-200);
        }
        
        .navbar-brand {
            font-weight: 900;
            font-size: 1.6rem;
            letter-spacing: -0.5px;
            color: var(--primary) !important;
        }
        
        .navbar-brand img { height: 36px; margin-right: 8px; }
        
        .nav-link { font-weight: 600; font-size: 0.95rem; color: var(--gray-700) !important; transition: color 0.2s; }
        .nav-link:hover { color: var(--primary) !important; }
        
        .btn-nav-primary {
            padding: 10px 24px;
            font-weight: 700;
            border-radius: 12px;
            border: none;
            background: var(--primary);
            color: white !important;
            font-size: 0.9rem;
            transition: all 0.3s;
        }
        .btn-nav-primary:hover {
            background: var(--primary-dark);
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(37,99,235,0.4);
        }
        
        /* ===== HERO ===== */
        .hero-section {
            position: relative;
            background: linear-gradient(160deg, #0f172a 0%, #1e293b 50%, #0f172a 100%);
            color: white;
            padding: 80px 0 100px;
            overflow: hidden;
        }
        
        .hero-section::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -20%;
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(37,99,235,0.15) 0%, transparent 70%);
            border-radius: 50%;
        }
        
        .hero-section::after {
            content: '';
            position: absolute;
            bottom: -30%;
            left: -10%;
            width: 400px;
            height: 400px;
            background: radial-gradient(circle, rgba(124,58,237,0.1) 0%, transparent 70%);
            border-radius: 50%;
        }
        
        .hero-content { position: relative; z-index: 2; }
        
        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: rgba(255,255,255,0.1);
            border: 1px solid rgba(255,255,255,0.15);
            border-radius: 50px;
            padding: 8px 20px;
            font-size: 0.85rem;
            font-weight: 600;
            color: rgba(255,255,255,0.9);
            margin-bottom: 24px;
            backdrop-filter: blur(10px);
        }
        
        .hero-badge .badge-dot {
            width: 8px; height: 8px;
            background: #22c55e;
            border-radius: 50%;
            animation: pulse-dot 2s ease-in-out infinite;
        }
        
        @keyframes pulse-dot {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.4; }
        }
        
        .hero-title {
            font-size: clamp(2.2rem, 5vw, 3.5rem);
            font-weight: 900;
            line-height: 1.1;
            margin-bottom: 20px;
            letter-spacing: -1px;
        }
        
        .hero-title .highlight {
            background: linear-gradient(135deg, var(--accent), #fbbf24);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .hero-subtitle {
            font-size: 1.15rem;
            color: rgba(255,255,255,0.7);
            max-width: 540px;
            line-height: 1.7;
            margin-bottom: 36px;
        }
        
        /* Search Card */
        .search-card {
            background: white;
            padding: 28px;
            border-radius: 20px;
            box-shadow: 0 25px 60px rgba(0,0,0,0.12);
        }
        
        .search-card .input-group { border: 2px solid var(--gray-200); border-radius: 12px; overflow: hidden; transition: border-color 0.2s; }
        .search-card .input-group:focus-within { border-color: var(--primary); }
        .search-card .input-group-text { background: white; border: none; padding-left: 16px; color: var(--gray-500); }
        .search-card .form-control { border: none; padding: 14px 12px; font-size: 0.95rem; }
        .search-card .form-control:focus { box-shadow: none; }
        .search-card .form-control::placeholder { color: #94a3b8; }
        
        .btn-search {
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 12px;
            padding: 14px 28px;
            font-weight: 700;
            font-size: 0.95rem;
            transition: all 0.3s;
            width: 100%;
        }
        .btn-search:hover {
            background: var(--primary-dark);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(37,99,235,0.35);
        }
        
        /* Stats dans le hero */
        .hero-stats {
            display: flex;
            gap: 40px;
            margin-top: 48px;
        }
        
        .hero-stat-item { text-align: center; }
        .hero-stat-number { font-size: 1.8rem; font-weight: 900; color: white; }
        .hero-stat-label { font-size: 0.8rem; color: rgba(255,255,255,0.5); font-weight: 600; text-transform: uppercase; letter-spacing: 1px; }
        
        /* ===== SECTIONS ===== */
        .section-header { margin-bottom: 48px; }
        .section-tag {
            display: inline-block;
            background: rgba(37,99,235,0.08);
            color: var(--primary);
            font-size: 0.8rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            padding: 6px 16px;
            border-radius: 50px;
            margin-bottom: 12px;
        }
        .section-title {
            font-size: clamp(1.6rem, 3vw, 2.2rem);
            font-weight: 800;
            color: var(--dark);
            letter-spacing: -0.5px;
        }
        .section-subtitle { color: var(--gray-500); font-size: 1.05rem; margin-top: 8px; }
        
        /* ===== CATÉGORIES ===== */
        .category-card {
            background: white;
            border: 1px solid var(--gray-200);
            border-radius: 16px;
            padding: 24px 16px;
            text-align: center;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            height: 100%;
            text-decoration: none;
            color: var(--gray-700);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        
        .category-card:hover {
            transform: translateY(-6px);
            box-shadow: 0 12px 32px rgba(0,0,0,0.1);
            border-color: transparent;
            color: var(--primary);
        }
        
        .cat-icon {
            width: 64px;
            height: 64px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 26px;
            margin-bottom: 14px;
            color: white;
            transition: transform 0.3s;
        }
        
        .category-card:hover .cat-icon { transform: scale(1.1) rotate(-3deg); }
        
        .cat-name { font-weight: 700; font-size: 0.85rem; margin-bottom: 4px; }
        .cat-count { font-size: 0.75rem; color: var(--gray-500); font-weight: 500; }
        
        /* ===== COMMENT ÇA MARCHE ===== */
        .step-card {
            background: white;
            border: 1px solid var(--gray-200);
            border-radius: 20px;
            padding: 40px 30px;
            text-align: center;
            transition: all 0.3s;
            height: 100%;
        }
        
        .step-card:hover { transform: translateY(-4px); box-shadow: 0 12px 32px rgba(0,0,0,0.08); }
        
        .step-number {
            width: 56px;
            height: 56px;
            border-radius: 16px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 22px;
            font-weight: 800;
            margin: 0 auto 20px;
        }
        
        .step-card h5 { font-weight: 800; color: var(--dark); margin-bottom: 10px; }
        .step-card p { color: var(--gray-500); font-size: 0.95rem; line-height: 1.6; margin: 0; }
        
        /* ===== DERNIÈRES ANNONCES ===== */
        .ad-card {
            background: white;
            border: 1px solid var(--gray-200);
            border-radius: 16px;
            overflow: hidden;
            transition: all 0.3s;
            height: 100%;
            text-decoration: none;
            color: var(--gray-700);
            display: block;
        }
        
        .ad-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 32px rgba(0,0,0,0.1);
            color: var(--gray-700);
        }
        
        .ad-card-img {
            width: 100%;
            height: 180px;
            object-fit: cover;
            background: var(--gray-100);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--gray-500);
            font-size: 2rem;
        }
        
        .ad-card-body { padding: 18px; }
        .ad-card-category {
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: var(--primary);
            margin-bottom: 6px;
        }
        .ad-card-title { font-weight: 700; font-size: 1rem; color: var(--dark); margin-bottom: 8px; line-height: 1.3; }
        .ad-card-location { font-size: 0.85rem; color: var(--gray-500); }
        .ad-card-price { font-weight: 800; color: var(--primary); font-size: 1.1rem; margin-top: 10px; }
        
        /* ===== CTA ===== */
        .cta-section {
            background: linear-gradient(135deg, var(--dark) 0%, #1e293b 100%);
            border-radius: 24px;
            overflow: hidden;
            position: relative;
        }
        
        .cta-section::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -20%;
            width: 400px;
            height: 400px;
            background: radial-gradient(circle, rgba(37,99,235,0.2) 0%, transparent 70%);
            border-radius: 50%;
        }
        
        /* ===== FOOTER ===== */
        footer {
            background: var(--dark);
        }
        
        footer a { transition: color 0.2s; }
        footer a:hover { color: white !important; }
        
        .footer-social a {
            width: 40px; height: 40px;
            border-radius: 10px;
            background: rgba(255,255,255,0.08);
            display: inline-flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s;
        }
        .footer-social a:hover {
            background: var(--primary);
            transform: translateY(-2px);
        }
        
        /* ===== RESPONSIVE ===== */
        @media (max-width: 768px) {
            .hero-section { padding: 60px 0 80px; }
            .hero-stats { gap: 24px; justify-content: center; flex-wrap: wrap; }
            .hero-stat-number { font-size: 1.4rem; }
            .search-card { padding: 20px; }
        }
        
        /* ===== ANIMATIONS ===== */
        .fade-up {
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .fade-up.visible { opacity: 1; transform: translateY(0); }
    </style>
</head>
<body>
    <!-- ===== NAVBAR ===== -->
    <nav class="navbar navbar-expand-lg sticky-top">
        <div class="container">
            <a class="navbar-brand" href="{{ url('/') }}">
                <i class="fas fa-layer-group me-2"></i>Lunamars
            </a>
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center gap-2">
                    <li class="nav-item">
                        <a href="{{ url('/ads') }}" class="nav-link">Annonces</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ route('contact.index') }}" class="nav-link">Contact</a>
                    </li>
                    @if (Route::has('login'))
                        @auth
                            <li class="nav-item dropdown">
                                <a id="navbarDropdown" class="nav-link dropdown-toggle fw-bold" href="#" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    <i class="fas fa-user-circle me-1"></i> {{ Auth::user()->name }}
                                </a>
                                <div class="dropdown-menu dropdown-menu-end border-0 shadow-lg rounded-3" aria-labelledby="navbarDropdown">
                                    <a class="dropdown-item py-2" href="{{ url('/home') }}">
                                        <i class="fas fa-tachometer-alt me-2 text-primary"></i>Tableau de bord
                                    </a>
                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item py-2 text-danger" href="{{ route('logout') }}"
                                       onclick="event.preventDefault(); document.getElementById('logout-form').submit();">
                                        <i class="fas fa-sign-out-alt me-2"></i>Se déconnecter
                                    </a>
                                    <form id="logout-form" action="{{ route('logout') }}" method="POST" class="d-none">@csrf</form>
                                </div>
                            </li>
                        @else
                            <li class="nav-item">
                                <a href="{{ route('login') }}" class="nav-link fw-bold">
                                    <i class="fas fa-sign-in-alt me-1"></i> Connexion
                                </a>
                            </li>
                            @if (Route::has('register'))
                                <li class="nav-item ms-1">
                                    <a href="{{ route('register') }}" class="btn-nav-primary text-decoration-none">
                                        <i class="fas fa-user-plus me-1"></i> S'inscrire
                                    </a>
                                </li>
                            @endif
                        @endauth
                    @endif
                </ul>
            </div>
        </div>
    </nav>

    <!-- ===== HERO SECTION ===== -->
    <header class="hero-section">
        <div class="container hero-content">
            <div class="row align-items-center">
                <div class="col-lg-6 mb-5 mb-lg-0">
                    <div class="hero-badge">
                        <span class="badge-dot"></span>
                        Plateforme N°1 de services
                    </div>
                    <h1 class="hero-title">
                        Trouvez le prestataire<br>
                        <span class="highlight">idéal</span> près de chez vous
                    </h1>
                    <p class="hero-subtitle">
                        Bricolage, jardinage, aide à domicile, cours particuliers... 
                        Connectez-vous avec des professionnels de confiance partout dans le monde.
                    </p>
                    
                    <div class="hero-stats">
                        <div class="hero-stat-item">
                            <div class="hero-stat-number">{{ $totalAds }}+</div>
                            <div class="hero-stat-label">Annonces</div>
                        </div>
                        <div class="hero-stat-item">
                            <div class="hero-stat-number">{{ count($categoriesWithSubs) }}</div>
                            <div class="hero-stat-label">Catégories</div>
                        </div>
                        <div class="hero-stat-item">
                            <div class="hero-stat-number">3</div>
                            <div class="hero-stat-label">Îles couvertes</div>
                        </div>
                    </div>
                </div>
                
                <div class="col-lg-6">
                    <div class="search-card">
                        <h5 class="fw-bold mb-3 text-dark"><i class="fas fa-search me-2 text-primary"></i>Rechercher un service</h5>
                        <form action="{{ url('/ads') }}" method="GET">
                            <div class="mb-3">
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fas fa-search"></i></span>
                                    <input type="text" name="q" class="form-control" placeholder="Que recherchez-vous ? (ex: plombier, cours...)">
                                </div>
                            </div>
                            <div class="mb-3">
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fas fa-map-marker-alt"></i></span>
                                    <input type="text" name="location" class="form-control" placeholder="Ville ou île (ex: Moroni, Anjouan...)">
                                </div>
                            </div>
                            <div class="mb-3">
                                <select name="category" class="form-select" style="border: 2px solid var(--gray-200); border-radius: 12px; padding: 14px 12px; font-size: 0.95rem;">
                                    <option value="">Toutes les catégories</option>
                                    @foreach($categoriesWithSubs as $catName => $catData)
                                        <option value="{{ $catName }}">{{ $catName }} ({{ $catData['total'] }})</option>
                                    @endforeach
                                </select>
                            </div>
                            <button type="submit" class="btn-search">
                                <i class="fas fa-search me-2"></i>Rechercher
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- ===== CATÉGORIES POPULAIRES ===== -->
    <section class="py-5">
        <div class="container py-3">
            <div class="section-header text-center">
                <span class="section-tag">Services</span>
                <h2 class="section-title">Explorez nos catégories</h2>
                <p class="section-subtitle">Trouvez rapidement le service dont vous avez besoin</p>
            </div>
            <div class="row g-3">
                @foreach($categoriesWithSubs as $catName => $catData)
                    <div class="col-6 col-sm-4 col-md-3 col-lg-2">
                        <a href="{{ url('/ads?category=' . urlencode($catName)) }}" class="category-card">
                            <div class="cat-icon" style="background: {{ $catData['color'] }};">
                                <i class="{{ $catData['icon'] }}"></i>
                            </div>
                            <div class="cat-name">{{ $catName }}</div>
                            <div class="cat-count">{{ $catData['total'] }} annonce{{ $catData['total'] > 1 ? 's' : '' }}</div>
                        </a>
                    </div>
                @endforeach
            </div>
        </div>
    </section>

    <!-- ===== DERNIÈRES ANNONCES ===== -->
    @if($latestAds->count() > 0)
    <section class="py-5 bg-white">
        <div class="container py-3">
            <div class="d-flex justify-content-between align-items-end mb-4">
                <div class="section-header mb-0">
                    <span class="section-tag">Nouveautés</span>
                    <h2 class="section-title">Dernières annonces</h2>
                </div>
                <a href="{{ url('/ads') }}" class="btn btn-outline-primary rounded-pill fw-bold d-none d-md-inline-block" style="border-width: 2px;">
                    Voir tout <i class="fas fa-arrow-right ms-1"></i>
                </a>
            </div>
            <div class="row g-4">
                @foreach($latestAds as $ad)
                    <div class="col-sm-6 col-lg-4">
                        <a href="{{ url('/ads/' . $ad->id) }}" class="ad-card">
                            <div class="ad-card-img">
                                @if($ad->photos && count($ad->photos) > 0)
                                    <img src="{{ asset('storage/' . $ad->photos[0]) }}" alt="{{ $ad->title }}" style="width:100%;height:180px;object-fit:cover;">
                                @else
                                    <i class="fas fa-image"></i>
                                @endif
                            </div>
                            <div class="ad-card-body">
                                <div class="ad-card-category">{{ $ad->category }}</div>
                                <div class="ad-card-title">{{ Str::limit($ad->title, 50) }}</div>
                                <div class="ad-card-location">
                                    <i class="fas fa-map-marker-alt me-1"></i>{{ $ad->address ?? $ad->location ?? 'France' }}
                                </div>
                                @if($ad->price)
                                    <div class="ad-card-price">{{ number_format($ad->price, 0, ',', ' ') }} €</div>
                                @else
                                    <div class="ad-card-price" style="color: #22c55e;">Gratuit / Sur devis</div>
                                @endif
                            </div>
                        </a>
                    </div>
                @endforeach
            </div>
            <div class="text-center mt-4 d-md-none">
                <a href="{{ url('/ads') }}" class="btn btn-outline-primary rounded-pill fw-bold" style="border-width: 2px;">
                    Voir toutes les annonces <i class="fas fa-arrow-right ms-1"></i>
                </a>
            </div>
        </div>
    </section>
    @endif

    <!-- ===== COMMENT ÇA MARCHE ===== -->
    <section class="py-5">
        <div class="container py-3">
            <div class="section-header text-center">
                <span class="section-tag">Simple & rapide</span>
                <h2 class="section-title">Comment ça marche ?</h2>
                <p class="section-subtitle">Trouvez votre prestataire en 3 étapes</p>
            </div>
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="step-card">
                        <div class="step-number">1</div>
                        <h5>Recherchez</h5>
                        <p>Parcourez nos {{ count($categoriesWithSubs) }} catégories ou utilisez la barre de recherche pour trouver le service idéal.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="step-card">
                        <div class="step-number">2</div>
                        <h5>Comparez</h5>
                        <p>Consultez les profils, avis et tarifs des prestataires pour faire le meilleur choix.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="step-card">
                        <div class="step-number">3</div>
                        <h5>Contactez</h5>
                        <p>Échangez directement avec le prestataire via notre messagerie intégrée et réservez.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- ===== APPEL À L'ACTION ===== -->
    <section class="py-5">
        <div class="container">
            <div class="cta-section p-5 text-white position-relative">
                <div class="row align-items-center position-relative" style="z-index:2;">
                    <div class="col-lg-7">
                        <span class="section-tag" style="background: rgba(255,255,255,0.15); color: white;">Prestataires</span>
                        <h2 class="fw-bold mt-3 mb-3" style="font-size: clamp(1.5rem, 3vw, 2rem);">
                            Vous êtes un professionnel ?
                        </h2>
                        <p class="mb-4 opacity-75" style="font-size: 1.05rem; max-width: 500px;">
                            Rejoignez Lunamars et développez votre activité en trouvant de nouveaux clients. Inscription gratuite !
                        </p>
                        <div class="d-flex gap-3 flex-wrap">
                            <a href="{{ route('register') }}" class="btn btn-light btn-lg px-4 fw-bold rounded-3" style="color: var(--dark);">
                                <i class="fas fa-rocket me-2"></i>Devenir Prestataire
                            </a>
                            <a href="{{ route('contact.index') }}" class="btn btn-outline-light btn-lg px-4 fw-bold rounded-3">
                                <i class="fas fa-info-circle me-2"></i>En savoir plus
                            </a>
                        </div>
                    </div>
                    <div class="col-lg-5 text-center d-none d-lg-flex align-items-center justify-content-center">
                        <div style="font-size: 100px; opacity: 0.15;">
                            <i class="fas fa-handshake"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- ===== FOOTER ===== -->
    <footer class="text-white pt-5 pb-4 mt-5">
        <div class="container">
            <div class="row g-4 mb-4">
                <div class="col-lg-4">
                    <h5 class="fw-bold mb-3">
                        <i class="fas fa-layer-group me-2"></i>Lunamars
                    </h5>
                    <p style="color: rgba(255,255,255,0.5); line-height: 1.7; font-size: 0.9rem;">
                        La plateforme de mise en relation entre particuliers et prestataires de services. 
                        Trouvez le bon prestataire en quelques clics, partout dans le monde.
                    </p>
                    <div class="footer-social d-flex gap-2 mt-3">
                        <a href="#" class="text-white"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" class="text-white"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="text-white"><i class="fab fa-twitter"></i></a>
                        <a href="#" class="text-white"><i class="fab fa-youtube"></i></a>
                    </div>
                </div>
                <div class="col-6 col-lg-2">
                    <h6 class="fw-bold mb-3">Navigation</h6>
                    <ul class="list-unstyled" style="font-size: 0.9rem;">
                        <li class="mb-2"><a href="{{ url('/') }}" class="text-decoration-none" style="color: rgba(255,255,255,0.5);">Accueil</a></li>
                        <li class="mb-2"><a href="{{ url('/ads') }}" class="text-decoration-none" style="color: rgba(255,255,255,0.5);">Annonces</a></li>
                        <li class="mb-2"><a href="{{ route('contact.index') }}" class="text-decoration-none" style="color: rgba(255,255,255,0.5);">Contact</a></li>
                    </ul>
                </div>
                <div class="col-6 col-lg-2">
                    <h6 class="fw-bold mb-3">Compte</h6>
                    <ul class="list-unstyled" style="font-size: 0.9rem;">
                        <li class="mb-2"><a href="{{ route('login') }}" class="text-decoration-none" style="color: rgba(255,255,255,0.5);">Connexion</a></li>
                        <li class="mb-2"><a href="{{ route('register') }}" class="text-decoration-none" style="color: rgba(255,255,255,0.5);">Inscription</a></li>
                    </ul>
                </div>
                <div class="col-lg-4">
                    <h6 class="fw-bold mb-3">Informations légales</h6>
                    <ul class="list-unstyled" style="font-size: 0.9rem;">
                        <li class="mb-2"><a href="{{ url('/legal/terms') }}" class="text-decoration-none" style="color: rgba(255,255,255,0.5);">Conditions d'utilisation</a></li>
                        <li class="mb-2"><a href="{{ url('/legal/privacy') }}" class="text-decoration-none" style="color: rgba(255,255,255,0.5);">Politique de confidentialité</a></li>
                        <li class="mb-2"><a href="{{ url('/legal/mentions') }}" class="text-decoration-none" style="color: rgba(255,255,255,0.5);">Mentions légales</a></li>
                        <li class="mb-2"><a href="{{ url('/legal/cookies') }}" class="text-decoration-none" style="color: rgba(255,255,255,0.5);">Politique de cookies</a></li>
                    </ul>
                </div>
            </div>
            <hr style="opacity: 0.1;">
            <div class="d-flex flex-column flex-md-row justify-content-between align-items-center gap-2" style="color: rgba(255,255,255,0.4); font-size: 0.85rem;">
                <p class="mb-0">&copy; {{ date('Y') }} Lunamars. Tous droits réservés.</p>
                <p class="mb-0">Fait avec <i class="fas fa-heart text-danger"></i> en France</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Scroll animations -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                    }
                });
            }, { threshold: 0.1 });
            
            document.querySelectorAll('.fade-up').forEach(el => observer.observe(el));
        });
    </script>
</body>
</html>